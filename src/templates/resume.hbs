<!DOCTYPE html>
<html>

<head>
  <title>{{basics.name}} - Resume</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <style>
    html {
      font-family: 'Roboto', sans-serif;
      background: #fff;
      font-size: 11pt;
      line-height: 1.65;
      color: #2c2c2c;
    }

    body {
      margin: 0;
      padding: 0;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      font-family: 'Roboto', sans-serif;
      font-weight: 500;
      color: #0b1f3a;
      margin: 0;
      line-height: 1.3;
    }

    h1 {
      font-size: 2.25rem;
      margin-bottom: 0.75rem;
      letter-spacing: -0.02em;
    }

    h2 {
      font-size: 1.35rem;
      margin-bottom: 1.25rem;
      letter-spacing: 0;
    }

    h3 {
      font-size: 1.1rem;
      margin-bottom: 0.4rem;
      font-weight: 500;
    }

    p {
      padding: 0;
      margin: 0 0 1rem 0;
      font-size: 1rem;
      line-height: 1.7;
    }

    a {
      color: #0b1f3a;
      text-decoration: none;
      border-bottom: 1px solid transparent;
      transition: border-color 0.2s ease;
    }

    a:hover {
      border-bottom-color: #0b1f3a;
    }

    ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    *,
    \n *::before,
    \n *::after {
      box-sizing: border-box;
    }

    /* Layout Classes */
    .container {
      max-width: 750px;
      margin: 0 auto;
      padding: 3rem 2.5rem;
    }

    @media print {
      .container {
        padding: 2.5rem 3rem;
        max-width: 100%;
      }
    }

    .header {
      margin-bottom: 2.5rem;
      padding-bottom: 1.75rem;
      border-bottom: 1.5px solid #0b1f3a;
    }

    .header-name {
      font-size: 2.25rem;
      color: #0b1f3a;
      margin-bottom: 0.6rem;
      font-weight: 500;
    }

    .header-label {
      font-size: 1.05rem;
      color: #555;
      margin-bottom: 1.5rem;
      font-weight: 400;
    }

    .contact-info {
      display: flex;
      flex-wrap: wrap;
      gap: 1.25rem;
      font-size: 0.9rem;
      color: #555;
      line-height: 1.5;
    }

    .section {
      margin-bottom: 2.25rem;
      page-break-inside: avoid;
    }

    .section-title {
      font-size: 1.35rem;
      color: #0b1f3a;
      margin-bottom: 1.5rem;
      padding-bottom: 0.6rem;
      border-bottom: 1px solid #d0d0d0;
      font-weight: 500;
    }

    .summary-text {
      font-size: 1rem;
      line-height: 1.75;
      color: #3a3a3a;
      margin: 0 0 0.5rem 0;
    }

    .item {
      margin-bottom: 2.25rem;
      page-break-inside: avoid;
    }

    .item-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 0.6rem;
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .item-title {
      font-size: 1.1rem;
      color: #0b1f3a;
      margin: 0;
      font-weight: 500;
    }

    .item-date {
      font-size: 0.85rem;
      color: #666;
      font-weight: 400;
    }

    .item-subtitle {
      font-size: 0.95rem;
      color: #555;
      margin-bottom: 0.85rem;
      font-weight: 400;
    }

    .item-summary {
      font-size: 0.95rem;
      line-height: 1.7;
      color: #3a3a3a;
      margin-bottom: 0.75rem;
    }

    .highlights {
      margin-top: 0.85rem;
      padding-left: 1.35rem;
      list-style: disc;
    }

    .highlights li {
      margin-bottom: 0.6rem;
      line-height: 1.7;
      color: #3a3a3a;
      font-size: 0.95rem;
    }

    .education-item {
      margin-bottom: 1.75rem;
      page-break-inside: avoid;
    }

    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1.75rem;
    }

    .skill-category {
      margin-bottom: 1.25rem;
    }

    .skill-name {
      font-size: 1rem;
      color: #0b1f3a;
      margin-bottom: 0.5rem;
    }

    .skill-keywords {
      font-size: 0.95rem;
      color: #555;
      line-height: 1.5;
    }
  </style>
</head>

<body>
  <div class="container">
    <header class="header">
      <h1 class="header-name">{{basics.name}}</h1>
      <p class="header-label">{{basics.label}}</p>
      <div class="contact-info">
        {{#if basics.email}}
        <div><a href="mailto:{{basics.email}}">{{basics.email}}</a></div>
        {{/if}}
        {{#if basics.phone}}
        <div>{{basics.phone}}</div>
        {{/if}}
        {{#if basics.url}}
        <div><a href="{{basics.url}}" target="_blank" rel="noopener noreferrer">{{removeProtocol basics.url}}</a></div>
        {{/if}}
        {{#each basics.profiles}}
        <div><a href="{{this.url}}" target="_blank" rel="noopener noreferrer">{{this.network}}</a></div>
        {{/each}}
      </div>
    </header>

    {{#if basics.summary}}
    <section class="section">
      <h2 class="section-title">Profile</h2>
      <p class="summary-text">{{basics.summary}}</p>
    </section>
    {{/if}}

    {{#if work.length}}
    <section class="section">
      <h2 class="section-title">Experience</h2>
      {{#each work}}
      <div class="item">
        <div class="item-header">
          <h3 class="item-title">{{this.position}}</h3>
          <div class="item-date">{{formatDate this.startDate}} - {{#if this.endDate}}{{formatDate
            this.endDate}}{{else}}Present{{/if}}</div>
        </div>
        <div class="item-subtitle">{{this.name}}</div>
        {{#if this.summary}}
        <div class="item-summary">
          <p>{{this.summary}}</p>
        </div>
        {{/if}}
        {{#if this.highlights.length}}
        <ul class="highlights">
          {{#each this.highlights}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
        {{/if}}
      </div>
      {{/each}}
    </section>
    {{/if}}

    {{#if education.length}}
    <section class="section">
      <h2 class="section-title">Education</h2>
      {{#each education}}
      <div class="education-item">
        <div class="item-header">
          <h3 class="item-title">{{this.studyType}} in {{this.area}}</h3>
          <div class="item-date">{{getYear this.startDate}} - {{getYear this.endDate}}</div>
        </div>
        <div class="item-subtitle">{{this.institution}}</div>
      </div>
      {{/each}}
    </section>
    {{/if}}

    {{#if skills.length}}
    <section class="section">
      <h2 class="section-title">Skills</h2>
      <div class="skills-grid">
        {{#each skills}}
        <div class="skill-category">
          <h3 class="skill-name">{{this.name}}</h3>
          <div class="skill-keywords">
            {{join this.keywords " • "}}
          </div>
        </div>
        {{/each}}
      </div>
    </section>
    {{/if}}
  </div>
</body>

</html>